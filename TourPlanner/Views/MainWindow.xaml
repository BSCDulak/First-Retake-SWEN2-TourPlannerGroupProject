<Window x:Class="SWEN2_TourPlannerGroupProject.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SWEN2_TourPlannerGroupProject"
        mc:Ignorable="d"
        
        xmlns:viewModels="clr-namespace:SWEN2_TourPlannerGroupProject.ViewModels"
        xmlns:userControls="clr-namespace:SWEN2_TourPlannerGroupProject.Views.UserControls"
        xmlns:subViews="clr-namespace:SWEN2_TourPlannerGroupProject.Views.Subviews"

        Title="MainWindow" Height="450" Width="800">

    <Window.DataContext>
        <viewModels:MainWindowViewModel></viewModels:MainWindowViewModel>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
            <userControls:MenuBar ></userControls:MenuBar>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="4*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="4*"></RowDefinition>
            </Grid.RowDefinitions>
            <userControls:SubTabButtons DataContext="{Binding SubTabButtonsViewModel}" Grid.ColumnSpan="2"></userControls:SubTabButtons>
            <userControls:ToursList DataContext="{Binding ToursListViewModel}" Grid.Row="1"></userControls:ToursList>
            <Button Grid.Column="1" Grid.Row="1">Details of Tour goes here</Button>
        </Grid>

    </Grid>
</Window>
